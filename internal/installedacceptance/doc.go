// @import { Module } from "github.com/spice-framework/spice/annotation/modulith"

// Package installedacceptance proves the packaged daemon and terminal workflow
// through real generated executables rather than in-process test doubles. The
// release-artifact boundary additionally drives independently verified native
// bytes through PTY/ConPTY and a bounded terminal emulator.
//
// @Module
package installedacceptance
